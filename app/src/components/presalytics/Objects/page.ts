// import Widget from './widget'
// import Plugin from './plugin'

// export default class Page {
//   kind: string
//   name: string
//   widgets: Array<Widget>
//   plugins: Array<Plugin>

//   constructor(page: any) {
//     this.kind = page.kind
//     this.name = page.name
//     this.widgets = page.widgets.map( (cur: any) => new Widget(cur))
//     this.plugins = page.plugins?.length > 0 ? page.plugins.map( (cur: any) => new Plugin(cur)) : []
//   }
// }