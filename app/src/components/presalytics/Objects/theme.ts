// import Plugin from './plugin'

// export default class Theme {
//   name: string
//   kind: string
//   data: any
//   plugins: Array<Plugin>

//   constructor(theme: any) {
//     this.name = theme.name
//     this.kind = theme.kind
//     this.data = theme.data ? theme.data : {}
//     this.plugins = theme.plugins?.length > 0 ? theme.plugins.map( (cur: any) => new Plugin(cur)) : []    
//   }
// }